// AspectRatio: constrains children to a fixed width:height ratio. Thin
// re-export of @radix-ui/react-aspect-ratio's Root; pass `ratio` (e.g. 16/9).
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Direct alias of the Radix Root (no extra styling added here).
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
